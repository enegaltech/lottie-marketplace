// Source: {{sourceUrl}} — {{license}}{{attributionNote}}
import { DotLottieReact } from '@lottiefiles/dotlottie-react';

interface {{componentName}}Props {
  size?: number;
  className?: string;
}

export function {{componentName}}({ size = {{size}}, className }: {{componentName}}Props) {
  return (
    <DotLottieReact
      src="{{src}}"
      autoplay
      loop
      className={className}
      style={{ width: size, height: size }}
    />
  );
}

export default {{componentName}};
